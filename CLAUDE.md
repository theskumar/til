# CLAUDE.md — AI Assistant Guide for theskumar/til

## Project Overview

This is a personal **TIL (Things I Learned)** repository — a curated knowledge base of technical
learnings with an automated static site and RSS feed. It is inspired by
[jakelazaroff/til](https://github.com/jakelazaroff/til/).

- **Content language:** Markdown
- **Automation language:** JavaScript (Node.js)
- **CI/CD:** GitHub Actions (`.github/workflows/build.yml`)
- **RSS:** Published at `https://saurabh-kumar.com/til/rss.xml`

---

## Repository Structure

```
til/
├── til.md                  # Primary notes file — all dated entries live here
├── build-site.js           # Builds static HTML site into _site/
├── package.json            # NPM deps: marked (^17.0.3), he (^1.2.0)
├── README.md               # Auto-generated index (do not edit below the --- line)
├── .github/
│   └── workflows/
│       └── build.yml       # CI: builds site and deploys to GitHub Pages
├── docs/                   # Meta-documentation
├── fts/                    # Notes on full-text search
├── github/                 # Notes on GitHub
├── react/                  # Notes on React
└── vscode/                 # Notes on VS Code
```

### Category folders

Individual, polished TIL articles live in topic subfolders. Each file:
- Has a kebab-case filename (e.g., `pg_textsearch.md`)
- Starts with a single `# H1 Title` (used for README index and RSS titles)
- Contains a focused, self-contained write-up

---

## Content Conventions

### Adding content

**Quick note** — append a dated bullet to `til.md`:

```
- DD Mon YYYY. Your note here.
  Optional continuation line (indent with exactly 2 spaces).
```

**Standalone article** — create `<category>/<slug>.md` starting with a `# Title` heading:

```
react/use-memo-pitfalls.md
vscode/multi-cursor-tricks.md
```

Push to `main` and CI automatically builds the site and deploys to GitHub Pages.
For local preview: `npm install && node build-site.js && npx http-server _site -p 8787`

### Parser details
- Date regex: `/^- (?<day>\d{1,2}) (?<month>[A-Za-z]{3}) (?<year>\d{4})/`
- Continuation lines must use exactly **2 spaces** — anything else terminates the note.
- `# Month YYYY` section headers in `til.md` are cosmetic; they are not parsed.
- Category detection is folder-based: `<category>/<slug>.md` → category in site nav.
- RSS titles come from the first `# Heading` in each article file.

---

## Automation

### build-site.js

Parses `til.md` and article files, generates a full static site in `_site/`:
- Home page with recent weeks, articles, and tag cloud
- Weekly pages with notes grouped by week (notes sorted newest-first)
- Article pages rendered from category folder markdown files
- Tag pages grouping notes by hashtag
- RSS feed (`rss.xml`)

### CI/CD Pipeline (`.github/workflows/build.yml`)

Triggers:
- Push to `main`
- Weekly cron: every Sunday at 00:00 UTC
- Manual `workflow_dispatch`

Steps:
1. **Checkout** with full history (`fetch-depth: 0`)
2. **`npm install`** — install dependencies
3. **`node build-site.js`** — generate static site
4. **Deploy to GitHub Pages** via `upload-pages-artifact` + `deploy-pages`

---

## Development Workflows

### Branch conventions

- `main` — production branch; CI runs on push
- `claude/<description>-<id>` — AI assistant feature branches

---

## File Naming Conventions

| Artifact | Convention | Example |
|---|---|---|
| Category folders | lowercase | `react/`, `fts/` |
| Article files | kebab-case | `setstate-is-async.md` |
| JS scripts | kebab-case | `build-site.js` |

---

## Key Files — Do Not Edit Manually

| File | Reason |
|---|---|
| `_site/` | Auto-generated by `build-site.js`; deployed by CI |

---

## Dependencies

| Package | Version | Use |
|---|---|---|
| `marked` | `^17.0.3` | Markdown → HTML for site and RSS |
| `he` | `^1.2.0` | HTML entity encoding for RSS feed |

---

## Notes for AI Assistants

- **Adding content**: Always add quick notes to `til.md` using the `- DD Mon YYYY.` format. Standalone articles go in a category subfolder with a `# Title` header.
- **Do not edit `_site/`** — it is generated by `build-site.js` and deployed by CI.
- **Date format is strict**: `DD Mon YYYY` (e.g., `20 Feb 2026`). The parser uses the regex `/^- (?<day>\d{1,2}) (?<month>[A-Za-z]{3}) (?<year>\d{4})/`.
- **Continuation lines** in `til.md` must use exactly 2-space indentation; anything else terminates the note.
- **Category detection** is folder-based: a file at `<category>/<slug>.md` appears under its category in the site.
- **RSS titles** come from the first `# Heading` in each article file.
